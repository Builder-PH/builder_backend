{
  "name": "builder_backend",
  "version": "1.0.0",
  "description": "A backend repository for builder",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nirel Ibarra and M Lee",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  }
}
